Map (JavaScript)
├── Basics
│ ├── Key-value pairs
│ ├── Keys can be any type
│ ├── Insertion order preserved
│ ├── Size: map.size
│ └── Mutating methods (set, delete, clear)
│
├── Creation
│ ├── new Map()
│ ├── new Map([[key,value], ...])
│ ├── Object → Map (Object.entries)
│ └── Map → Object (Object.fromEntries)
│
├── Core Methods
│ ├── set(key, value)
│ ├── get(key)
│ ├── has(key)
│ ├── delete(key)
│ └── clear()
│
├── Iteration
│ ├── for...of ([key, value])
│ ├── forEach((value, key)=>{})
│ ├── keys()
│ ├── values()
│ └── entries()
│
├── Complex Keys
│ ├── Objects as keys
│ ├── Functions as keys
│ └── Keys unique by reference
│
├── Set vs Object Comparison
│ ├── Any type as key
│ ├── Easy iteration
│ ├── Optimized for performance
│ └── Clear key-value semantics
│
├── Performance (Big-O)
│ ├── set: O(1)
│ ├── get: O(1)
│ ├── has: O(1)
│ └── iteration: O(n)
│
├── Useful Patterns
│ ├── Merge Maps [...m1, ...m2]
│ ├── Filter [...map].filter()
│ ├── Transform [...map].map()
│ └── Reverse key-value pairs
│
├── Serialization
│ ├── Not JSON directly
│ ├── Convert to array first
│ └── new Map(JSON.parse())
│
└── WeakMap (Advanced Cousin)
├── Only object keys
├── Weak references (GC-friendly)
├── No iteration
└── Private data storage
