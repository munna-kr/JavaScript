Array
│
├── Properties
│   └── length
│
├── Add / Remove
│   ├── push
│   ├── pop
│   ├── unshift
│   ├── shift
│   ├── splice
│   └── slice
│
├── Searching
│   ├── indexOf
│   ├── lastIndexOf
│   ├── includes
│   ├── find
│   └── findIndex
│
├── Logic Check
│   ├── some
│   └── every
│
├── Iteration / Transformation
│   ├── forEach
│   ├── map
│   ├── filter
│   ├── reduce
│   ├── reduceRight
│   ├── flat
│   └── flatMap
│
├── Ordering
│   ├── sort
│   └── reverse
│
├── Conversion
│   ├── join
│   └── toString
│
└── Creation Helpers
    ├── Array.from
    ├── Array.of
    └── Array.isArray

//===>
                  ┌────────────────────────────┐
                  │        Object.prototype    │
                  │ ─────────────────────────  │
                  │ toString()                 │
                  │ hasOwnProperty()           │
                  │ valueOf()                  │
                  └──────────────▲─────────────┘
                                 │ (inherits)
                  ┌──────────────┴─────────────┐
                  │       Array.prototype      │
                  │ ─────────────────────────  │
                  │ push()                     │
                  │ pop()                      │
                  │ map()                      │
                  │ filter()                   │
                  │ reduce()                   │
                  │ forEach()                  │
                  │ slice()                    │
                  │ concat()                   │
                  └──────────────▲─────────────┘
                                 │ (inherits)
                  ┌──────────────┴─────────────┐
                  │          arr = [1,2,3]     │
                  │  (your actual array object)│
                  └────────────────────────────┘
